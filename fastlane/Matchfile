# frozen_string_literal: true

storage_mode('git')
git_url(ENV['MATCH_GIT_URL'])

type('development')

app_identifier([ENV['APP_IDENTIFIER'], ENV['WIDGET_IDENTIFIER']])
username(ENV['APPLE_ID'])

include_mac_in_profiles(true)
